async function recuperarSenha(e){e.preventDefault();const msg=document.getElementById('recuperarMsg');try{const r=await fetch('http://localhost:5000/api/auth/forgot-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:document.getElementById('email').value})});const d=await r.json();if(!r.ok)throw new Error(d.message);msg.innerHTML=`<div class="alert alert-success rounded-4">${d.message}${d.token_recuperacao?`<br><small class="d-block mt-2">Ambiente de desenvolvimento: token <code>${d.token_recuperacao}</code></small>`:''}</div>`;}catch(err){msg.innerHTML=`<div class="alert alert-danger rounded-4">${err.message||'Erro ao recuperar senha'}</div>`;}}
async function redefinirSenha(e){e.preventDefault();const msg=document.getElementById('resetMsg');try{const r=await fetch('http://localhost:5000/api/auth/reset-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token:document.getElementById('token').value,nova_senha:document.getElementById('novaSenha').value})});const d=await r.json();if(!r.ok)throw new Error(d.message);msg.innerHTML=`<div class="alert alert-success rounded-4">Senha redefinida com sucesso. <a href="login.html">Fazer login</a></div>`;}catch(err){msg.innerHTML=`<div class="alert alert-danger rounded-4">${err.message||'Erro ao redefinir senha'}</div>`;}}
document.addEventListener('DOMContentLoaded',()=>{document.getElementById('recuperarForm')?.addEventListener('submit',recuperarSenha);document.getElementById('resetForm')?.addEventListener('submit',redefinirSenha);});
